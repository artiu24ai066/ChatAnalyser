# ── Existing dependencies (unchanged) ────────────────────────────────────────
streamlit
pandas
numpy
matplotlib
seaborn
wordcloud
urlextract
emoji

# ── NLP dependencies ──────────────────────────────────────────────────────────

# transformers — Hugging Face pipeline API.
# Runs all three models: sentiment, emotion, and sarcasm.
transformers

# torch — PyTorch backend required by all transformer models.
torch

# sentencepiece — tokenizer required by XLM-RoBERTa (sentiment + emotion models).
sentencepiece

# accelerate — speeds up HuggingFace inference, suppresses device warnings.
accelerate

# scipy — used internally by transformers for softmax / special functions.
# Required by amaan00z/sarcasm_xlmr (sarcasm model).
scipy
